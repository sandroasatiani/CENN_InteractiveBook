using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class textiskodi : MonoBehaviour {

	// Use this for initialization
	void Start () {
		gameObject.GetComponent<Text>().text = "ასდ";
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
